bgra8_srgb wsi_screen { store, out_layout = present_src };
//rgba8_srgb screen;
rgba32_sfloat color;
rgba8_unorm normal;
d24un_or_32sf_spl_att_sfb depth_buffer { load = clear };

albedo {
	depth_stencil depth_buffer;

	out color;
	out normal;
}

lighting {
	in color;
	in normal;

	//in depth_buffer;
	//out screen;
	out wsi_screen;

	//resolve screen : wsi_screen;
}

[
	albedo,
	lighting,

	color_attachment_output,
	fragment_shader,

	color_attachment_write,
	input_attachment_read,

	by_region
]