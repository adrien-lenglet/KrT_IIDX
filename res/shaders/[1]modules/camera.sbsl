set camera {
	mat4 vp;
	mat4 view;
	mat4 view_normal;
	mat4 proj;
	mat4 inv_proj;
	vec2 ratio;
	float a, b;
	float far, near;
	//push_constant mat4 vp;
};

vec4 camera_project_point(vec3 p)
{
	return camera.vp * vec4(p, 1.0);
}